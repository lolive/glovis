// MapLayerLoadingCallback.java defines the interface for reporting progress
// made while reading map layer files.
//--------------------------------------------------------------------------

interface MapLayerLoadingCallback
{
    public void incrementFileReadCounter();
}
